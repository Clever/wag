version: 2
jobs:
  build:
    working_directory: ~/go/src/github.com/Clever/wag
    docker:
      - image: cimg/go:1.21
    environment:
      CIRCLE_ARTIFACTS: /tmp/circleci-artifacts
      CIRCLE_TEST_REPORTS: /tmp/circleci-test-results
      NODE_VERSION: v7.10.1
      NODE_DISTRO: linux-x64
    steps:
    # This is horrible, but node v7 is so outdated that there aren't better alternatives.
      - run:
          command: |-
            wget https://nodejs.org/download/release/$NODE_VERSION/node-$NODE_VERSION-$NODE_DISTRO.tar.gz 
            sudo mkdir -p /usr/local/lib/nodejs 
            sudo tar -xf node-$NODE_VERSION-$NODE_DISTRO.tar.gz -C /usr/local/lib/nodejs   
            sudo chmod -R 777 /usr/local/lib/nodejs
            rm node-$NODE_VERSION-$NODE_DISTRO.tar.gz
            echo export PATH=/usr/local/lib/nodejs/node-$NODE_VERSION-linux-x64/bin:"$PATH" >> "$BASH_ENV"
            source "$BASH_ENV"
            npm install -g jsdoc-to-markdown@^4.0.0
            echo export PATH=`pwd`/bin:"$PATH" >> "$BASH_ENV"
            source "$BASH_ENV"


      - run:
          command: cd $HOME && git clone --depth 1 -v https://github.com/Clever/ci-scripts.git && cd ci-scripts && git show --oneline -s
          name: Clone ci-scripts
      - checkout
      - setup_remote_docker
      - run:
          command: mkdir -p $CIRCLE_ARTIFACTS $CIRCLE_TEST_REPORTS
          name: Set up CircleCI artifacts directories
      - run: echo $PATH
      # Currently have to run this in shell so that it can discover the new PATH changes. 
      - run:
          command: |-
            echo $PATH
            make install_deps
            make build
            make test
      - run: if [ "${CIRCLE_BRANCH}" == "master" ]; then make release && $HOME/ci-scripts/circleci/github-release $GH_RELEASE_TOKEN release; fi;
