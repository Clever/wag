responses:
  BadRequest:
    description: "Bad Request"
    schema:
      $ref: "#/definitions/Test"

definitions:
  Test:
    type: object
    properties:
      message:
        type: string
    required:
      - message
