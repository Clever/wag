responses:
  BadRequest:
    description: "Bad Request"
    schema:
      $ref: "#/definitions/Test"

definitions:
  Test:
    type: object
    properties:
      msg:
        type: string
    required:
      - msg
