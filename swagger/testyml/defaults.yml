responses:
  BadRequest:
    description: "Bad Request"
    schema:
      $ref: "#/definitions/Error"
  InternalError:
    description: "Internal Error"
    schema:
      $ref: "#/definitions/Error"

paths:
  /path:
    get:
      operationId: op
      responses:
        404:
          description: "Other response"

definitions:
  Error:
    type: object
    properties:
      msg:
        type: string
